import { View, Text } from 'react-native'
import React from 'react'

const Whislist = () => {
  return (
    <View style={{flex:1,justifyContent:"center",alignItems:"center"}}>
      <Text style={{color:"red"}}>Whislist</Text>
    </View>
  )
}

export default Whislist